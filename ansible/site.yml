---
- name: Configure SPA deployment environment
  hosts: localhost
  connection: local
  gather_facts: false
  vars:
    ansible_python_interpreter: /usr/bin/python3

  vars_files:
    - vars/config.yml

  roles:
    - app-config
    - security-hardening
